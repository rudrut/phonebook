{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Phonebook backend",
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.12",
    "static": "^2.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ../part2-notes/ && npm run build --prod && cp -r build ../notes-backend",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "proxy": "http://localhost:3001",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rudrut/phonebook.git"
  },
  "author": "Rudi Rutanen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rudrut/phonebook/issues"
  },
  "homepage": "https://github.com/rudrut/phonebook#readme"
}
